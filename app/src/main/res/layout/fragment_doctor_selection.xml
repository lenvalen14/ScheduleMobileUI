<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Selected Specialty -->
    <TextView
        android:id="@+id/tvSelectedSpecialty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Chuyên khoa: Tim mạch"
        android:textColor="@color/blue_007b"
        android:textSize="14sp"
        android:textStyle="bold"
        android:background="@drawable/specialty_badge_bg"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:drawableStart="@drawable/ic_specialty_placeholder"
        android:drawablePadding="8dp" />

    <!-- Header Text -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Chọn bác sĩ phù hợp để thăm khám"
        android:textColor="@color/text_secondary"
        android:textSize="16sp"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:lineSpacingExtra="4dp" />

    <!-- Doctors List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvDoctors"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical" />

    <!-- Loading -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="50dp"
        android:visibility="gone" />

    <!-- No Data -->
    <TextView
        android:id="@+id/tvNoDoctors"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="50dp"
        android:text="Không có bác sĩ khả dụng"
        android:textColor="@color/text_secondary"
        android:textSize="16sp"
        android:visibility="gone" />

</LinearLayout> 