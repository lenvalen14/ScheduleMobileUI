<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_default">

    <!-- Top Bar -->

    <ImageView
        android:id="@+id/btnBack"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="24dp"
        android:layout_marginStart="24dp"
        android:src="@drawable/ic_arrow_back"
        app:tint="@color/text_primary"
        android:contentDescription="Back"
        android:layout_alignParentStart="true" />

    <!-- Avatar -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/ivAvatar"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_below="@id/btnBack"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:src="@drawable/sample_profile_image"
        app:civ_border_width="2dp"
        app:civ_border_color="@color/primary_blue" />

    <!-- Tên người dùng -->
    <TextView
        android:id="@+id/tvFullName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivAvatar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="John Doe William"
        android:textColor="@color/text_primary"
        android:textSize="20sp"
        android:textStyle="bold"
        android:fontFamily="@font/inter_semibold" />

    <!-- Email -->
    <TextView
        android:id="@+id/tvEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvFullName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="john.doe@example.com"
        android:textColor="@color/text_secondary"
        android:textSize="14sp" />

    <!-- Phone -->
    <TextView
        android:id="@+id/tvPhone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvEmail"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="4dp"
        android:text="+84 123 456 789"
        android:textColor="@color/text_secondary"
        android:textSize="14sp" />

    <!-- Danh sách menu -->
    <LinearLayout
        android:id="@+id/menuList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvPhone"
        android:layout_above="@id/bottom_navigation"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp">

        <include layout="@layout/item_profile_menu" android:id="@+id/itemMyAppointments" />
        <include layout="@layout/item_profile_menu" android:id="@+id/itemPersonalDetails" />
        <include layout="@layout/item_profile_menu" android:id="@+id/itemPassword" />
        <include layout="@layout/item_profile_menu" android:id="@+id/itemSettings" />
        <include layout="@layout/item_profile_menu" android:id="@+id/itemLogout" />
    </LinearLayout>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        android:elevation="8dp"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="unlabeled"
        app:itemActiveIndicatorStyle="@style/App.Custom.Indicator" />

</RelativeLayout>
